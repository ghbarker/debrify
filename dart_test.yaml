tags:
  golden:
    description: >
      Pixel-diff screenshots. GitHub ubuntu runs them in the goldens job with
      retries plus `[golden]` allowlist entries (Linux raster diffs are
      expected). Do not delete the PNG files. Local: flutter test --tags golden.
